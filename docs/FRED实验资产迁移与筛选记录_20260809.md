# FRED 实验资产迁移与筛选记录（2026-08-09）

## 1. 整理范围

本轮先在目标服务器完成 FRED 专属实验资产筛选和旧仓库收口，随后将全部保留
资产传输到主服务器的 `Grid_Mamba/experiments/runs/`。传输和校验完成后，目标
服务器中的对应资产实体已经删除；主服务器是当前唯一规范存储位置。

本轮不修改论文、不重新训练模型、不重新计算正式测试或 Runtime，也不处理工作区
级多模型 FRED 事件数自适应 Runtime。

## 2. 最终保留结构

```text
experiments/runs/
├── ev_flying/
│   └── baseline/EF45/best_iou_seed37.pt
├── fred/
│   └── ablation/
│       └── FRED_SC12_GS_SCALED/
└── cross_dataset/
    └── EF45_to_FRED/
        ├── final/
        │   └── fred_full/
        ├── comparisons/
        │   ├── area1250_full/
        │   └── area1250_sample700k/
```

大型实验资产位于被忽略的 `experiments/runs/`，不会随普通 Git 提交或拉取传输。
EF45 到 FRED 直接引用正式 EF45 checkpoint，不保留第二份模型实体。注册表、
校验值和本文档纳入版本控制。

## 3. 正式 FRED 实验

`FRED_SC12_GS_SCALED` 完成 50 epoch，训练入口返回码为 0。迁移时完整保留同一
正式实验内部的 checkpoint、训练状态、配置、日志、完整/受限评测和 Runtime
结果，不对内部 smoke 或历史评测子目录继续拆分。

迁移核对结果：

| 项目 | 数值 |
|---|---:|
| 文件数 | 60 |
| 普通文件总字节数 | 58,063,994 |
| 软链接数 | 0 |
| 递归 SHA-256 摘要 | `4e92b8c0f1a97e95da73f9f1d27f3111d38efae527324f9c720350dbe2f60c9d` |

主要 checkpoint：

| 文件 | SHA-256 |
|---|---|
| `best_iou_seed37.pt` | `aa8308bcf60bab10a477925d3c82888a6cfcb5690d86826c1a788cb65eb643a4` |
| `best_loss_seed37.pt` | `e51ae6becc39f8d0306af30a1dac59004e3a4998c1e86feb0e12251b4e4858e4` |
| `latest_train_state_seed37.pt` | `53ddd976d24f20aa2742c39f88d1232022b09e375d9a89a485c5f126f9a65e98` |

## 4. EF45 到 FRED 的最终跨数据集结果

三组保留结果都使用 EF45 正式 `best_iou` checkpoint，未在 FRED 上重新训练。
来源 checkpoint 的 SHA-256 为：

```text
6e50c9854d82151441c584e6136d893f0580050380e754727b08f47816284ba7
```

去除重复 checkpoint 后，`EF45_to_FRED` 目录包含 17 个普通文件、314,540
字节，递归 SHA-256 摘要为
`2e2e4f16775455d2f42e24c10ce6f2ae45ff9b4f75f0c0df2867f132bc16d9d7`。

### 4.1 最终采用结果

`final/fred_full` 是论文最终采用的 `FRED_segmentation` 完整测试划分结果。它
使用分块分类头避免超大样本 OOM，完整处理 529 个样本和 2,516,672,665 个事件。

| 指标 | 数值 |
|---|---:|
| 阈值 | 0.03 |
| IoU | 0.6351188167（63.51%） |
| Acc | 0.6989575909（69.90%） |
| Pd | 0.8778166809 |
| Fa | 0.1446461322 |

以上数值对应在 FRED test 上以 0.01 步长扫描得到的最优 IoU 阈值。来源域
EV-Flying 固定阈值 0.39 的结果仍保存在原始 `metrics.json` 中，不改写历史数据。

### 4.2 两组对照

| 目录 | 协议 | 样本/事件数 | 最优 IoU | Acc |
|---|---|---:|---:|---:|
| `comparisons/area1250_full` | 小目标子集，全事件 | 160 / 795,413,091 | 0.4725895992 | 0.5709298218 |
| `comparisons/area1250_sample700k` | 同一子集，每样本最多 70 万事件 | 160 / 91,850,312 | 0.2893754697 | 0.4301593068 |

`area1250` 是按每个 8 秒块的平均裁剪后目标框面积小于 1250 px² 生成的小目标
子集。两组结果用于保留全事件与强制降采样的对照关系，不作为最终主结果。

## 5. 删除记录

用户确认失败、中断、不可复现开发实验和被完整版本替代的前置结果不再保留。

| 分类 | 删除内容 | 字节数 | 原因 |
|---|---|---:|---|
| 失败训练 | `fred_segmentation_area1250/ev_flying_config` | 37,388,905 | 第 1 epoch 的 101/650 处中断 |
| 中断训练 | `fred_segmentation_area1250/ev_flying_config_exact_full_eval` | 60,078,968 | 第 33 epoch 的 188/650 处中断 |
| 对应日志 | 两份顶层 area1250 训练日志 | 5,021,053 | 仅对应已删除训练 |
| 不可复现开发实验 | `FRED_SC12_GS_SCALED_CROP800_FT_1E` | 91,553,465 | Crop800 参数在当前代码和 Git 历史中无可验证实现 |
| 不可复现开发实验 | `FRED_SC12_GS_SCALED_CROP800_FT_5E_LR5E5_FROM3E` | 38,567,633 | 同上，且完整 FRED test 未完成 |
| 失败评测 | `ev_flying_ef45_to_fred_full_6e50c985` | 204,368 | 运行到 434/529 时 CUDA OOM，由完整分块版本替代 |
| 重复 checkpoint | `EF45_to_FRED/source_checkpoint/ef45_best_iou_seed37_6e50c985.pt` | 7,469,654 | 与正式 EF45 `best_iou` 字节完全一致 |

目标服务器筛选阶段删除 232,814,392 字节；集中到主服务器后再删除一份重复
checkpoint，FRED 本轮累计删除 240,284,046 字节。以上资产均不能依赖 Git
恢复。

## 6. 迁移与验证结果

迁移过程逐项执行：复制到规范目标、比较文件数和普通文件字节数、计算相对路径
递归 SHA-256、执行递归差异检查，全部一致后才删除旧源实体。

目标服务器到主服务器的第二次传输继续使用相同摘要复核。最终核对结果：

- 主服务器的 FRED 与跨数据集规范目录共 77 个普通文件、58,378,534 字节；
- 迁移范围内没有软链接或断裂软链接；
- 旧 `fred_ablation`、`fred_segmentation_area1250` 和 `cross_dataset` 来源目录均已
  删除；
- 目标服务器 `Grid_Mamba_GitHub/experiments/runs/` 中的 FRED 和 EF45 到 FRED
  实体已在主服务器校验完成后删除；
- `FRED_SC12_GS_SCALED/best_iou_seed37.pt` 在当前核心模型上
  `strict=True` 加载成功，共 96 个状态项；
- 正式 EF45 checkpoint 在当前核心模型上 `strict=True` 加载成功，共 96 个状态项；
- 两次加载均使用 `torch.load(..., weights_only=True)`，未执行 checkpoint 中的任意
  Python pickle 对象；
- 两套处理后 FRED 数据仍可读取：完整 `FRED_segmentation` 为 1815/408/529，
  area1250 为
  650/110/160；
- 大型资产按设计被 `.gitignore` 排除，不进入普通 Git 提交。

## 7. 后续边界

- 跨数据集来源 checkpoint 已与主服务器正式 EF45 `best_iou` 做 SHA-256 和逐字节
  比较，两者完全一致；重复副本已删除，注册表直接引用正式 EF45 唯一实体。
- 目标服务器的 `Grid_Mamba_GitHub` 继续保留代码仓库，但不再保存这批大型实验
  资产，避免跨服务器分散管理。
- 迁移后的历史 `test_config.yaml`、`metrics.json` 和日志继续保留实验发生时的旧绝对
  路径，不改写证据文件；活动入口和注册表使用规范路径。
- 目标服务器没有工作区级 `fred_event_count_adaptivity_v1` 资产，本轮未补跑。
- `configs/evisseg_fred_area1250.yaml` 仍可作为数据集实验配置保留，但其旧训练产物
  已明确删除，不再登记为正式资产。

## 8. 辅助工具与分析资产补充收口

2026-08-09 继续复查目标服务器后，补齐了四类此前未进入正式 FRED 实验目录的
资产：

1. 将 `FULL_SC12` 轨迹后处理下的 7 个实例分割评测文件迁入规范目录；其
   AP 54.43%、AP50 76.56%、AP75 52.92%、mCov 73.01% 和 mWCov 79.66%
   与冻结论文 Discussion 完全一致，逐文件 SHA-256 已加入正式校验表；
2. 将原始 FRED 下载、HDF5 事件窗口提取和 5 个数据审查/可视化脚本分别归入
   `dataset/preprocessing/fred/` 与 `tools/analysis/fred/`，去除旧服务器绝对
   路径和默认代理；目标服务器对应旧脚本已经删除；
3. 将 HDF5 filter `36559` 所需的两个动态库迁入被 Git 忽略的
   `tools/hdf5_ecf/plugin/`，完成源端哈希和本机动态加载验证；版本、哈希和
   发布边界记录在 `docs/环境依赖版本记录.md`；
4. 将 bbox 统计、最新 label audit、精选 RGB/bbox 审查和 RGB/event 对比共
   194 个文件集中到 `experiments/analysis/fred/`。四组资产通过 checksum
   目录对比后，目标服务器源目录以及两个早期 label audit、一个 smoke 目录
   已删除。

完成迁移核验后，用户进一步筛选分析产物，删除了本机
`experiments/analysis/fred/label_audit/` 和
`experiments/analysis/fred/rgb_bbox_audit/`，共 144 个文件、45,683,285
字节。这两组目标服务器源目录此前已在迁移校验后删除，因此本次删除不可依赖
Git 或目标服务器恢复。当前只保留 bbox 统计和精选 RGB/event 对比，共 50 个
文件。随后同步删除 `fred_label_audit.py` 和
`visualize_fred_rgb_bboxes.py` 两个独立生成工具；后者被 RGB/event 对比使用的
公共解析函数已收缩到无命令入口的 `_rgb_common.py`，保留工具入口不受影响。

官方原始 `datasets/FRED/` 约 191 GB，本轮按用户决定继续保留在目标服务器，
没有传输或删除。本机已有的 `FRED_segmentation` 与
`FRED_segmentation_area1250` 是模型可直接读取的处理后数据，不属于原始数据。
